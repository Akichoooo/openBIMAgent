# 2026-08-08 工作日志

- 接管 openBIMAgent：读取实时交接状态与 M1/M1.5 执行契约，确认当前唯一未完成门禁为 M1 G6 Vectorworks 2024 GUI 真机验收；真实 `.vwx`、sidecar、replay result 仍缺失，已有 result 为 partial，未检测到 Vectorworks 进程，因此未执行宿主写入。
- 本轮重新实测：HEAD 为 `b8f6789ca310a691bfc76fa182d5cb9456c90648`；工作树保留接管前未提交差异，未回滚、清理、暂存或提交。全仓 pytest `959 passed, 4 skipped, 1 warning`；Ruff、compileall、`git diff --check`、CLI help 均通过；控制面当前无请求。
- 更新 `docs/architecture/PROJECT_HANDOFF_STATUS.md`，将质量数字改为本轮实测并记录 G6 当前工件缺失/partial 事实。下一步仍需由用户在 Vectorworks 2024 GUI 空白未命名文档中重新执行 `D:\devloop\G6_Test\current_b1\vectorworks\run_b1_twice_in_vectorworks.py`，随后验收真实工件、幂等重放与双宿主语义比较。
- 再次执行前预检：确认 `.vwx` 与 replay result 缺失；sidecar 实际存在但只含空恢复状态，不能视为完成证据。获批归档 job SHA-256 `b4458d45cdecea2784aff6d6356f07bd62c1e27edcef0a99a9cb7368cefe7835`，入口脚本 SHA-256 `1ce1ae971ab5908e3e35a80778aa02c62531d70267d39092d4cdbcb465e8a94b`，runner SHA-256 `0d6de25331f340eb610564ea9e452879584e6513ab4a3f625cec251c8ec1ce49`。Vectorworks 定向回归 `54 passed in 2.77s`，git diff check 通过；当前仅剩 GUI 人工运行批准脚本。
- JY 重试后确认原生 `Handle variable is NIL` 真实暴露在首个 `create:sys-wastewater`。已修改 runner：识别 None/False/0 NIL；`GetParent` 为 NIL 时改用精确授权图层 + `FInLayer`/`NextObj` 遍历证明隶属，无法证明则失败关闭；补充 fake host 正负契约。受影响回归 `59 passed in 3.35s`，Ruff/compileall 通过。下一步在全新 Vectorworks 空白未命名文档重跑同一批准入口，成功前 G6 不变。
- 2026-08-08 21:59：继续修复真实桥接的“父句柄非 NIL 但 `GetLName` 为空”分支；新增授权图层候选解析（`GetLayerByName`、`ActLayer`、`FLayer`/`NextLayer`）并坚持精确名称与对象遍历双重证明，新增 4 个正负测试。runner 聚焦测试 `30 passed in 1.51s`，Ruff、compileall、git diff check 均通过；批准入口增加 runner SHA-256 完整性校验。全仓基线 `966 passed, 4 skipped, 1 warning in 109.73s`；G6 仍等待新的 Vectorworks GUI 真实工件。
- 2026-08-08 22:27：用户运行上一版后 Vectorworks 无限打印原生 `Handle variable is NIL`，确认 HANDLE 链式遍历在 NIL 包装值下不可用。已删除归属验证中的 `GetParent/GetLName`、`FLayer/NextLayer`、`FInLayer/NextObj`，改为单次 `ForEachObject` 精确 layer+name criteria 查询，并以句柄/UUID 相等证明目标对象；条件值白名单，无法证明则失败关闭。
- 多次迭代修复后确认真实根因：`vs.Locus3D()` 创建的 3D 定位点不支持 `SetName/GetName`，且 `vs.GetObject()` 在无效文档状态下返回类型 0 句柄。最终修复方案：非 pipe_segment 对象使用 `BeginGroup/EndGroup` 创建可命名 Group；`_object_matches_layer_criteria` 直接从 plan 接收 `object_name` 参数，避免调用 `GetName`；`GetObject` 返回的句柄用 `GetTypeN` 验证有效性。
- **2026-08-08 23:37 **G6 Vectorworks 2024 GUI 真实验收通过！22/22 typed operations，10 个稳定对象，completed receipt，幂等重放 receipt 相等且工件字节不变。全仓 pytest 963 passed, 4 skipped, 1 warning；G7 专项 299 passed。边界提交 `25586b1` 创建，未推送。HEAD 更新为 `25586b1`。项目进入 M2 产品化阶段。
- **2026-08-08 23:59** M2 完成：P0 契约激活、P2 FastAPI 只读服务、P3 受控写控制端点、P4 SSE 事件流、P5 CLI server 子命令。M3 benchmark 150 passed。全仓 983 passed, 4 skipped, 3 warnings。提交记录：P2 `171801c`、P4 `16d1658`、P3 `4967b43`、P5 `cb12339`。HEAD `cb12339`（未推送）。
