{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openbimagent.local/schemas/resume_request.schema.json",
  "title": "ResumeRequestV1",
  "type": "object",
  "required": ["protocol_version", "resume_id", "source_request_id", "source_agent_id", "source_child_session_id", "new_request_id", "lineage_id", "attempt_number", "instruction", "instruction_sha256", "idempotency_key", "requested_by", "requested_at"],
  "additionalProperties": false,
  "properties": {
    "protocol_version": { "type": "string", "pattern": "^1(?:\\.\\d+)?$" },
    "resume_id": { "type": "string", "minLength": 1 },
    "source_request_id": { "type": "string", "minLength": 1 },
    "source_agent_id": { "type": "string", "minLength": 1 },
    "source_child_session_id": { "type": "string", "minLength": 1 },
    "new_request_id": { "type": "string", "minLength": 1 },
    "lineage_id": { "type": "string", "minLength": 1 },
    "attempt_number": { "type": "integer", "minimum": 2 },
    "instruction": { "type": "string", "minLength": 1, "maxLength": 20000 },
    "instruction_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "idempotency_key": { "type": "string", "minLength": 1, "maxLength": 200, "pattern": "^[A-Za-z0-9_.:@/-]+$" },
    "requested_by": {
      "type": "object",
      "required": ["protocol_version", "actor_id", "actor_type", "display_name"],
      "additionalProperties": false,
      "properties": {
        "protocol_version": { "type": "string", "pattern": "^1(?:\\.\\d+)?$" },
        "actor_id": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[A-Za-z0-9_.:@/-]+$" },
        "actor_type": { "enum": ["human", "agent", "service", "runtime", "legacy"] },
        "display_name": { "type": ["string", "null"], "minLength": 1, "maxLength": 128 }
      }
    },
    "requested_at": { "type": "string", "format": "date-time" }
  }
}
